# To be used in Portainer

version: "3.9"

volumes:
  db_data:
    driver: local

services:
  db:
    image: "postgres:14.2"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready"]
      interval: 10s
      timeout: 5s
      retries: 5
    env_file:
      - "stack.env"
    environment:
      PGUSER: "bobbot"
      PGPASSWORD: "bobbot"
      PGDATABASE: "bobbot"
      POSTGRES_USER: "bobbot"
      POSTGRES_PASSWORD: "bobbot"
      POSTGRES_DB: "bobbot"
    volumes:
      - "db_data:/var/lib/postgresql/data"
  
  bot:
    image: "ghcr.io/ryghub/bobbot:latest"
    restart: unless-stopped
    depends_on:
      - db
    env_file:
      - "stack.env"
    environment:
      DATABASE_URL: "postgres://bobbot:bobbot@db/bobbot"
